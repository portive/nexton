/**
 * Make your own customizations here
 */
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {},
  "exclude": [
    "node_modules",
    /**
     * Ignore pages because that's just for a demo and is not part of final
     * package.
     */
    "pages"
  ]
}
